extends ../layout

block content
	h1 Welcome to Canvas, #{user.username}
	button(type="button") create new drawing
	h3 Enter Existing Lobby
	form(method="get" action="/users/#{userID}/edit")
		input(type="submit" value="edit profile")